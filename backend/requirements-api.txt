# ResumeAI Web API — install AFTER base requirements.txt
# pip install -r requirements.txt && pip install -r requirements-api.txt
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9
requests>=2.31.0
